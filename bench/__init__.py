"""The benchmark harness (SPEC Phase 6): load generator, local Docker driver, charts.

A package (like `chaos`) so its modules run with `python -m bench.<module>` from the
repo root or from the worker image, where Phase 8 runs the load generator on AWS.
"""
